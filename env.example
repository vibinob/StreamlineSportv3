# Database Configuration (MySQL 5.1)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=streamline_sport

# Server Configuration
SERVER_PORT=3111
SERVER_HOST=0.0.0.0
NODE_ENV=development

# Frontend Configuration
FRONTEND_PORT=YYYY
FRONTEND_HOST=0.0.0.0
API_URL=http://localhost:XXXX

# Public Club ID (if needed by your application)
PUBLIC_CLUB_ID=CLUB_ID


# Frontend API Base URL (used by Vite for direct API calls)
# Note: VITE_ prefix is required for Vite to expose this to the frontend
VITE_API_BASE_URL=http://localhost:XXXX

# Optional: Connection Pool Settings
# DB_POOL_LIMIT=10
# DB_CONNECT_TIMEOUT=10000

